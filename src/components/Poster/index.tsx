import styles from './index.module.scss'

const Poster = () => {
  return (
    <div className={ styles.Poster }>
      <h1 className={ styles.Poster__title }>Program name</h1>
    </div>
  )
}

export default Poster
